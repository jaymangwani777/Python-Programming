a=(1,4,6,7)

print(type(a))

a = () # empty tuple
a = (1,) # tuple with only one element needs a comma
a = (1,7,2) # tuple with more than one element

b=("one",2,True,566,78,78,"ninety")

print(b.count(78))
# b.count (78): b count (78) will return number of times 78 occurs in b.

print(b.index(True))
# b.index will return the index of the value  it will return the first index of that value if 3 of these same values are stored it will return the first one 

